#pragma once

// SD
const int SD_PIN = 10;
const String MAPPINGFILE = "/.state/mapping";
const String STATEFILE = "/.state/state";

// Buttons
const uint8_t BTN_LIGHT_IDLE = 10; // brightness of buttons while idle, between 0 and 255
const uint8_t BTN_LIGHT_ACTIVE = 50; // brightness of buttons during click, between 0 and 255

// LCD
const uint8_t LCD_CONTRAST = 5;
